import React, { FC } from 'react';
import MainLayouts from '../components/layouts/MainLayouts';

const Home: FC = () => {
  return (
    <MainLayouts>
      MainLayouts
    </MainLayouts>
  )
};

export default Home;
