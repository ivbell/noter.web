import { Box, Button } from "@chakra-ui/react";
import React, { FC } from "react";

const AddAbility: FC = () => {
  return (
    <Box m={1}><Button variant={'ghost'}>Add ability</Button></Box>
  )
}

export default AddAbility